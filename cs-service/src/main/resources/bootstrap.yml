spring:
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        serviceId: CONFIG-CENTER
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8201/eureka/
